# Evidence

Place benchmark outputs here.

Recommended files:

```text
prompt_ensemble_benchmark.json
prompt_ensemble_benchmark.csv
prompt_ensemble_benchmark_report.md
human_eval_template.csv
```

The pilot numbers in the study are based on `prompt_ensemble_benchmark.json`.
